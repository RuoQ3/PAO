# ==============================================================================
# PAO — Process Aspen Optimization
# 依赖清单（基于 Python 3.x / Windows）
# ==============================================================================

# ------------------------------------------------------------------------------
# 核心运行依赖
# ------------------------------------------------------------------------------
pywin32==311                  # Aspen Plus COM 自动化 (pythoncom, win32com.client)
numpy==2.2.6                  # 数值计算、LHS 拉丁超立方采样加速
scikit-optimize==0.10.2       # 贝叶斯优化代理模型 (GP / RF / ET / GBRT)，qEHVI 路径的 fallback
scikit-learn==1.7.2           # 机器学习算法（scikit-optimize 依赖）
scipy==1.15.3                 # 科学计算（scikit-optimize / Pareto 超体积计算依赖）

# BoTorch 多目标 BO 后端（可选加速，不安装时自动退化为 skopt GP）
# 安装：pip install torch botorch gpytorch
# 不安装时 surrogate_model: qEHVI/NEHVI 将退化为 GP（skopt）
torch>=2.0.0                  # PyTorch，BoTorch 依赖，自动检测 CUDA
botorch>=0.10.0               # qEHVI / qNEHVI / FuRBO Trust Region
gpytorch>=1.11.0              # BoTorch 依赖的高斯过程库
PyYAML==6.0.3                 # YAML 配置文件解析
python-dotenv==1.0.0          # 从 .env 文件加载环境变量（LLM key 管理）

# ------------------------------------------------------------------------------
# Web API 后端（H4-1）
# ------------------------------------------------------------------------------
fastapi==0.115.6              # HTTP API 框架
uvicorn==0.32.1               # ASGI 服务器（backend/server.py 启动入口）
sse-starlette==2.1.3          # Server-Sent Events 支持（/api/session/{id}/stream）
python-multipart==0.0.18      # FastAPI 表单/文件上传依赖

# ------------------------------------------------------------------------------
# 可视化（仅脚本工具 scripts/ 使用，主流程可选）
# ------------------------------------------------------------------------------
matplotlib==3.10.9

# ------------------------------------------------------------------------------
# 测试框架
# ------------------------------------------------------------------------------
pytest==9.0.3
colorama==0.4.6               # 终端彩色输出（pytest 依赖）

# ------------------------------------------------------------------------------
# 传递依赖（由上述包自动安装，固定版本确保可复现性）
# ------------------------------------------------------------------------------
joblib==1.5.3
threadpoolctl==3.6.0
packaging==26.2
pyparsing==3.3.2
cycler==0.12.1
contourpy==1.3.2
kiwisolver==1.5.0
fonttools==4.63.0
pillow==12.2.0
python-dateutil==2.9.0.post0
six==1.17.0
pyaml==26.2.1
pygments==2.20.0
iniconfig==2.3.0
pluggy==1.6.0
typing_extensions==4.15.0

# ------------------------------------------------------------------------------
# 注意事项
# ------------------------------------------------------------------------------
# 1. pywin32 仅支持 Windows，在非 Windows 平台驱动层无法运行
# 2. scikit-optimize 为可选降级：若未安装，代码自动回退到随机采样
# 3. numpy 为可选加速：若未安装，LHS 采样回退到随机网格
# 4. 安装命令：pip install -r requirements.txt

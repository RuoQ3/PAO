[pytest]
testpaths = tests
# importmode=importlib 让 pytest 用标准 import 机制，
# 兼容 tests/ 子目录有/没有 __init__.py 混用的情况。
addopts = --import-mode=importlib
markers =
    aspen: 需要真实 Aspen Plus 环境的集成测试（本地运行，CI 跳过）
